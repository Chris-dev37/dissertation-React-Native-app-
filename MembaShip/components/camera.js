import React from 'react';
import { Modal, View, Text } from 'react-native';

const CameraFunc = props => {
    return (
        <Modal visable={props.visable}>
            <View>
                <Text>aaa</Text>
            </View>
        </Modal>
    );
};

// const styles = StyleSheet.create({

// });

export default CameraFunc;